Bus-mediated `clear` now requires a `--for-task` / `for_task` target-task label (e.g. `tmux-tell#286`) and synthesises an atomic `/clear` + `/rename "<Chamber> <task>"` pair — clearing the recipient's session and then relabelling the fresh session to its dispatch-time task identity (#286). This keeps the chamber's persistent name free so a later `claude --resume <Chamber>` resolves unambiguously instead of colliding with the orphaned same-named session a bare `/clear` leaves behind. The label is constrained (single line, starts alphanumeric, ≤80 chars of `[A-Za-z0-9 #/._-]`) so nothing a caller types can smuggle a second pasted command; `for_task` on any command other than `clear` is rejected fail-loud, and `clear` without it is rejected (forward-only — the bosun/qm→pilot edges are updated in lockstep, no deprecation cycle). The post-`/compact` settle-gate now also covers `/clear`, so the follow-up `/rename` lands in the settled new session rather than being swallowed mid-reset.
