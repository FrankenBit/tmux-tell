Fixed post-paste verification on Win11-rendered Claude panes. The cursor-anchored
input-cleared check now recognizes the same `>` plus non-breaking-space prompt
variant as the pre-paste classifier, preventing false `delivered_in_input_box`
outcomes when Claude consumes a message without visibly echoing its verify token.
