Capped `--workers=4` on all three remaining `gremlins-*` jobs in
`mutation.yml` (`render`, `store`, `tmuxio`). The `gremlins-tmuxio`
job peaked at 9.7 GB on 2026-06-28 ~05:00 UTC and triggered a
systemd-oomd cascade that crashed alcatraz (3.5h offline). Same
defense-in-depth shape that `gremlins-cli` already had per the prior
characterisation run. Render + store haven't OOM'd historically but
the precautionary cap is free.
