The agent manual's self-`/compact` section now spells out *why* the atomic `sleep` + `resume_with` verb is preferred over filing a hand-off and then hand-typing `/compact`: the two-step path has a stall-window (hand-off staged, reset never fired — the "stalled at the execution seam" pattern), and the atomic verb collapses both into one call so nothing is left half-done. This is the discipline framing #615 asked for; the underlying atomic mechanism (a `/compact` row + a deferred `resume_with` row inserted together via `InsertMessagePair`) already shipped and is hardened by the #622 stability-gate.
