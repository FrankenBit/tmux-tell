- **Codex active turns no longer false-classify as `Idle`** (#590). The pane-state classifier gained a positive per-adapter `WorkingPattern` marker: codex renders a persistent `◦ Working (Ns • esc to interrupt)` status row throughout a turn, but its only per-second change is the elapsed counter, so a 200ms temporal-delta capture pair could read the frame as stable and the cursor-at-sentinel branch would classify the busy pane `Idle` — letting the observe-gate treat an active turn as paste-safe. The classifier now matches codex's working-status row (checked before the frame-change and cursor logic) and reports `Working` regardless of frame stability. Claude is unaffected — its `WorkingPattern` is empty and its animated spinner the temporal-delta already catches.
