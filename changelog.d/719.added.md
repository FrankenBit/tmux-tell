Added a `StateErrored` pane classification that detects Claude Code's terminal API-error chrome (`● API Error: <code> …`) and stops the mailman false-idling into a dead turn. When a request gives up, Claude Code leaves the error line painted but PRESERVES the composer `❯` prompt row, so the cursor-at-sentinel heuristic previously read the pane as Idle, the observe-gate flushed, and the delivery pasted into a failed turn as unsubmitted ghost text. Detection is live-scoped: the error line must sit in the current-turn region (strictly between the bottom-most transcript `❯`-prompt and the composer, so error phrasing quoted in scrollback cannot fire it) and carry a status code (`API Error: \d`), and it is suppressed while `esc to interrupt` is present in the footer — an active 529-retry stays classified Working, only a terminal (given-up) error classifies Errored. `StateErrored` is content-corrupting paste-unsafe (`IsPasteUnsafeForced`), so the observe-gate defers it and the mailman's pre-paste safety belt aborts a delivery, never force-overridable. Claude-only for now (codex parks the check via an empty `APIErrorMarker`); restart-mode / usage-limit variants remain additive slots pending real captures.
