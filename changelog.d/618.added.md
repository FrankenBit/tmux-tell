The mailman now **auto-resumes a chamber after a transient rate-limit** (#618): when the #448 self-observe probe sees `StateRateLimited`, it waits out the cooldown (banner `Retry-After` hint or the #613 exponential backoff, plus #543 jitter) then pastes `continue` to resume the interrupted turn — the prompt the operator typed by hand before. It rides the existing self-observe cadence (like #621's metabolism auto-clear), so it fires regardless of whether a bus message is queued; the probe loop is the verify-and-retry, ending the episode once the chamber leaves rate-limited. Bounded by `--rate-limit-resume-max-attempts` (default 5) before giving up and leaving the chamber for the operator. Only the #504 transient throttle is resumed — `StateUsageLimited` (#540 hard quota park) is left alone. Default-on with no don't-fight-upstream guard (no adapter auto-resumes natively today); `--rate-limit-resume-disabled` is the escape hatch. New counter `tmux_tell_rate_limit_resume_total{outcome,agent,provider}` (`attempt` / `recovered` / `gave_up`). Detection rides the existing `RateLimitPattern` substrate; the empirical Claude/Codex pane patterns remain a separate forcing-function-gated capture (#504/#540).
