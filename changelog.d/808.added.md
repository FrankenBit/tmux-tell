Added an independent systemd mailman observer. It sweeps expected per-agent
mailman units every 30 seconds and sends a one-shot conductor notice when an
enabled unit stays inactive or enters a restart loop, covering OOM/crash
failures that the mailman's self-observed freshness alert cannot detect.
